with "aunit.gpr";

project TextProcs is

   for Source_Dirs use ("src", "src/tests", "src/3rdparty");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Linker is
   end Linker;

   package Builder is
      for Switches ("ada") use ("-j4", "-C", "-s");
   end Builder;

   package Compiler is
      for Switches ("ada") use ("-fPIC", "-O", "-gnatn", "-O2");
      for Switches ("c") use ("-O3");
   end Compiler;

   for Languages use ("Ada", "C");

end TextProcs;

